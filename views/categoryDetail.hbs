<main>
  <h2>Category Details</h2>
  <section>
    <h3>Category Name: {{categoryName}}</h3>
    <p>Category Description: {{categoryDesc}}</p>
  </section>
  <section>
    {{#if products}}
      {{> productListTable products=products}}
    {{#else}}
      <h4>No Products under this category</h4>
    {{/if}}
  </section>
</main>
