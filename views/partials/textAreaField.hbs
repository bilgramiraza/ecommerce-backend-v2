<div>
  <label for="{{id}}">{{label}}</label>
  <textarea 
    id="{{id}}"
    name="{{id}}"
    value="{{value}}"
    class=""
    rows="5"
  >
  {{#if error}}
    <span>{{error}}</span>
  {{/if}}
</div>
